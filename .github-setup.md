# GitHubリポジトリ作成手順

1. https://github.com/new にアクセス
2. Repository name: "postorial" または "sns-app"
3. Private を選択
4. "Create repository" をクリック

5. 作成後、以下のコマンドを実行:
   git remote add origin https://github.com/ユーザー名/リポジトリ名.git
   git branch -M main
   git push -u origin main
